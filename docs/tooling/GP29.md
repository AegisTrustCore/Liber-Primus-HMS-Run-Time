# HMS GP29 Calculator

Status: **v0.1.1 FINAL PUBLIC CANDIDATE — release record in progress**

The active usability candidate adds a recommended English-letter mode and a visible, selectable Gematria Primus alphabet. Its scope is defined in [GP29_SCOPE_V0.1.1.md](GP29_SCOPE_V0.1.1.md), with a separate [v0.1.1 ordinary-user acceptance protocol](GP29_UAT_V0.1.1.md). The previously qualified v0.1.0 package remains an immutable historical subject, but its UAT path was superseded when this user-facing revision was requested.

The v0.1.0 feature contract remains frozen in [GP29_SCOPE_V0.1.0.md](GP29_SCOPE_V0.1.0.md). v0.1.1 does not change its rune table or calculation definitions; it makes ambiguous input behavior easier to understand and control.

## What it is

GP29 calculates explicit Gematria Primus prime values. It is an offline calculator, not a decoder, language detector, translation, or proof that an output is plaintext.

## Download

No public download is authorized yet. A local v0.1.1 engineering package has passed automated checks, reproducibility, and malware scans, but the formal candidate must be rebuilt from merged source and pass independent qualification. It must not become a GitHub or Patreon asset until clean-machine acceptance is recorded and the bound software gate is approved.

Planned release artifact: `HMS-GP29-v0.1.1-Windows-x64-portable.zip`

## Quick start

Inside the candidate package:

1. Double-click `HMS-GP29.exe` for the desktop interface.
2. Use English-letter mode for ordinary A-Z text, or select a row in the visible alphabet and insert its exact token or rune.
3. Choose the input mode and select **Calculate**.
4. Use **Export JSON** to save the complete deterministic result.
5. Power users can run `HMS-GP29-CLI.exe --help` or `HMS-GP29-CLI.exe self-test`.

Developer source remains runnable with:

```text
python scripts/hms_runtime.py self-test
python scripts/hms_runtime.py gp29 "ᚠᚢᚦ" --mode runes
python scripts/hms_runtime.py gp29 "F U/V TH" --mode tokens --job
```

## Supported systems

- Candidate desktop and CLI package: Windows 10/11 x64.
- Developer source: CPython 3.12 using the standard library.

The candidate executables do not require Python, `pip`, source checkout, an HMS account, or a network connection.

## Input rules

- Rune mode accepts the 29 frozen Unicode runes and whitespace.
- English-letter mode maps A through Z independently. `TH` becomes `T + H`, so an English H is never absorbed into the TH sound rune.
- Latin mode accepts continuous Latin text and applies a frozen longest-alias rule. For example, `THING` becomes `TH ING`; this is deterministic transliteration, not language interpretation.
- Token mode requires tokens separated by whitespace, commas, semicolons, or pipes.
- Accepted aliases include `U`/`V`, `C`/`K`, `I`/`J`, `IO`/`IA`, `S`/`Z`, and `NG`/`ING`.
- Auto mode chooses rune mode when it sees a supported rune, token mode when every separated token is explicit, and Latin mode otherwise.

## Frozen table

```text
ᚠ ᚢ ᚦ ᚩ ᚱ ᚳ ᚷ ᚹ ᚻ ᚾ ᛁ ᛄ ᛇ ᛈ ᛉ ᛋ ᛏ ᛒ ᛖ ᛗ ᛚ ᛝ ᛟ ᛞ ᚪ ᚫ ᚣ ᛡ ᛠ
```

Values are the first 29 primes, from 2 through 109. Canonical sounds and aliases are frozen in `hms_tools/gp29.py` and exercised by public tests.

## Output format

Every calculation opens on a compact dashboard: headline totals, normalized runes and tokens, an aggregate register table, and a per-rune breakdown. Raw JSON remains available in a secondary tab and through export. The underlying result includes a SHA-256 digest over canonical JSON and is declared by [gp29-result.schema.json](../../schemas/gp29-result.schema.json). Runtime mode wraps the calculation in `HMS_RUNTIME_JOB_V1` and `HMS_RUNTIME_RESULT_V1`.

The registers are defined without hidden parameters:

- `L = index`, from 0 through 28 in canonical GP29 order;
- `R = (-L) mod 29`;
- `prime` is the corresponding member of the first 29 primes;
- `N = prime mod 29`; and
- `Q = floor(prime / 29)`, so `prime = 29Q + N`.

## Limitations

- Latin segmentation is mechanical longest-alias transliteration, not a phonetic or linguistic judgment. Use token mode when exact segmentation matters.
- It does not select transforms, routes, keys, or candidate plaintext.
- `CALCULATION_ONLY` output is not research verification.
- The current desktop build targets Windows x64 only.

## Verify the candidate

The v0.1.1 candidate must contain internal `SHA256SUMS` and independently pass CLI self-test, deterministic Runtime-job execution, GUI headless self-test, internal checksum verification, reproducible build, security scan, and the [v0.1.1 ordinary-user acceptance run](GP29_UAT_V0.1.1.md).

The release manifest and public checksum remain authoritative only after human approval. Rebuilding after approval invalidates the approval.

## Developer build

```text
python -m pip install -r requirements-build.txt
python scripts/build_gp29_windows.py
```

The pinned build dependency is PyInstaller 6.22.0. The builder fixes the source epoch and Python hash seed, creates both standalone executables, writes internal checksums, and produces a deterministic ZIP envelope.

## Changelog

- `0.1.0-rc.1`: superseded engineering candidate; desktop, CLI, JSON export, offline help, and reproducible Windows package path.
- `0.1.0-rc.2` source lineage: frozen v0.1.0 input/output contract, continuous Latin mode, and explicit `L/R/prime/N/Q` registers.
- `0.1.0`: public-facing package identity; qualification and explicit release approval remain pending.
- `0.1.1-rc.1`: separates English letters from Latin sound clusters and adds the visible, selectable alphabet.
- `0.1.1-rc.2`: adds the results dashboard, quick examples, alphabet filtering, session history, copy actions, CSV export, and calculation shortcuts.
- `0.1.1`: promotes the accepted rc.2 feature set without changing the GP29 table or calculation definitions.
