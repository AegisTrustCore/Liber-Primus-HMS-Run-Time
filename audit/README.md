# Internal-Source Audit Interface

This public directory reports audit scope and status without publishing private source paths, raw notebooks, premium code, or Vault material.

The current intake measurement found 889 Runtime run directories and dozens of page-specific documents and experiment definitions. Counts do not imply independent evidence: generated reruns, duplicates, controls, and superseded artifacts are expected.

The first audit output will be a deduplicated claim inventory, not a bulk file export.
